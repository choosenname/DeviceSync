<Page
    x:Class="App1.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ScrollViewer>
            <StackPanel
                Spacing="25"
                Padding="30,0"
                VerticalAlignment="Center">

                <TextBlock x:Name="Address"/>

                <TextBox x:Name="RemoteAddress" PlaceholderText="Введите адрес" Text="192.168.0.1"/>
                <TextBox x:Name="LocalPort" PlaceholderText="Введите локальный порт" Text="5001"/>
                <TextBox x:Name="RemotePort" PlaceholderText="Введите удаленный порт" Text="5002"/>

                <Button x:Name="Message" Content="Сообщения" Click="Message_Clicked"/>
                </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
