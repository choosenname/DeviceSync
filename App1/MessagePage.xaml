<Page
    x:Class="App1.MessagePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>
        <ScrollViewer MinHeight="50" MaxHeight="150" Background="LightGray">
                <Border>
                    <TextBlock x:Name="Chat"/>
                </Border>
            </ScrollViewer>

            <TextBox x:Name="MessageView" PlaceholderText="MessageView" Text=""/>

            <Button 
                x:Name="SendMessageButton"
                Content="Send message"
                Click="SendMessage_Clicked"/>

        <Button 
                x:Name="SendFileButton" 
                Content="Send file" 
                Click="SendFile_Clicked"/>
    </StackPanel>
</Page>
